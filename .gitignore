# CMake build output — always generated, never committed.
/build/

# Compiled artifacts
*.exe
*.dll
*.obj
*.o
*.a
*.lib

# Throwaway verification worktrees (if ever created inside the repo)
/verify/

# Node tooling for the PDF build (regenerate with `npm install`)
tools/node_modules/
tools/package-lock.json
